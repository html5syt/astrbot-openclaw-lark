{
  "app_id": {
    "description": "飞书应用的 App ID",
    "type": "string",
    "hint": "在飞书开放平台 -> 应用信息 中获取"
  },
  "app_secret": {
    "description": "飞书应用的 App Secret",
    "type": "string",
    "hint": "在飞书开放平台 -> 应用信息 中获取，请妥善保管"
  },
  "domain": {
    "description": "飞书域名",
    "type": "string",
    "default": "feishu",
    "options": ["feishu", "lark"],
    "hint": "国内版选 feishu，海外版选 lark"
  },
  "auth_mode": {
    "description": "认证模式",
    "type": "string",
    "default": "tenant",
    "options": ["tenant", "user"],
    "hint": "tenant：以机器人（租户）身份调用 API（默认）；user：以用户 OAuth 身份调用 API，需通过 /feishu login 完成授权。可用 /feishu auth [tenant|user] 命令随时切换。"
  }
}
